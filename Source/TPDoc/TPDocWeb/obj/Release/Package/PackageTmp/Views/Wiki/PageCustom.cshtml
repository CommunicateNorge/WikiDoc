@model Dictionary<string, string>

<section id="allContent" class="content">
    <section id="manualContentPage">


        @if (@Model.ContainsKey("pageNav"))
        {
            <div class="pageNavDiv">
                @Html.Raw(@Model["pageNav"])
            </div>
        }
        <section class="manualContent">
            <h2>Custom tables</h2>
            <div>
                This page contains the contents of tables from the production environment uploaded every day with the other automaticly generated information.
                Use the embed link to embed the table into a page. See the FAQ for mor information.
            </div>
            @Html.Raw(Model["Manual_Content"])
        </section>
    </section>
</section>


<br />
<br />
