<h3>Version 1.0.0</h3>
<h4>Changed</h4>
<p>* Moved project from Standard Azure WebApp to SharePoint Provider Hosted App.</p>
<p>* Moved project to Rema sorce control</p>
<p>* Made changes to accommodate for concurrent editing of manual pages.</p>
<h4>Added</h4>
<p>* Added changelog</p>
<p>* Added new styling to tables with the <em>simpleTable</em> class.</p>
<p>* Added <em>Table</em> element to the MarkItUp Editor for ease of use.</p>
<h4>Removed</h4>
<p>* Removed role filtering from old role based security. Now uses SharePoint for authentication. New role hierarchy yet to be implemented.</p>

